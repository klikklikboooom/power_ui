mutation ChangePassword(
  $token: String!
  $newPassword: String!
  $retypePassword: String!
) {
  changePassword(
    token: $token
    newPassword: $newPassword
    retypePassword: $retypePassword
  ) {
    ...RegularUserResponse
  }
}
